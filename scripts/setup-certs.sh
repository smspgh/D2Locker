#!/bin/sh

# Setup SSL certificates from environment variables for Railway deployment
# This script is not needed since we're loading certificates directly in the app

echo "SSL certificate setup handled by application"